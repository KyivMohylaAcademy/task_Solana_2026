[package]
name = "item_nft"
version = "0.1.0"
description = "Metaplex NFT minting and burn gate for crafted items"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "item_nft"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []

[dependencies]
anchor-lang = { version = "0.32.1", features = ["init-if-needed"] }
anchor-spl = { version = "0.32.1", features = ["associated_token", "metadata", "token"] }
game_common = { path = "../../crates/game_common" }
resource_manager = { path = "../resource_manager", features = ["cpi"] }
